# splat-annotation
Tool to annotate tracking points

Depends on PySide2 https://wiki.qt.io/PySide2, which can be installed by:

```pip install pyside2```

Or from apt

```sudo apt install python3-pyside2.qtwidgets python3-pyside2.qtcore python3-pyside2.qtgui```

